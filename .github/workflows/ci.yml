name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions/setup-rust@v1
        with:
          rust-version: stable

      - name: Install dependencies for Tauri
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.0-dev build-essential curl wget libssl-dev libappindicator3-dev librsvg2-dev

      - name: Build Rust workspace
        run: cargo build --workspace --verbose

      - name: Run Rust tests
        run: cargo test --workspace --all-features --verbose

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Lint with Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      # Optional: Web frontend build (if Svelte/React etc.)
      # - name: Install Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #
      # - name: Build web frontend
      #   run: |
      #     cd web
      #     npm ci
      #     npm run build